{
  "commands": [
    "docker ps -a --format 'table {{.Names}}\t{{.Status}}'",
    "curl -s -o /dev/null -w 'api_http=%{http_code}' http://127.0.0.1:10002/ || true",
    "curl -s -o /dev/null -w ' ws_http=%{http_code}' http://127.0.0.1:10001/ || true"
  ]
}
