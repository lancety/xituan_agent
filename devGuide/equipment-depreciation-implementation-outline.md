# 设备折旧报表系统实施大纲

## 概述

本文档列出了设备折旧报表系统的完整实施步骤，包括数据库、后端和前端的所有开发任务。

---

## ✅ 阶段1：数据库迁移（已完成）

### 1.1 创建迁移文件
- ✅ **文件**: `1710000000201_add_equipment_depreciation_report_fields.sql`
- ✅ **内容**:
  - 添加 `equipment.initial_depreciation_method` 字段
  - 添加 `equipment_depreciation_records` 表的新字段：
    - `calculated_initial_data` (JSONB)
    - `is_committed`, `committed_at`, `committed_by`, `commit_note`
    - `is_adjusted`, `adjustment_reason`, `adjusted_at`
  - 添加相关索引

---

## ✅ 阶段2：类型定义更新（部分完成）

### 2.1 枚举更新 ✅
- ✅ **文件**: `equipment.type.ts` (后端和前端)
- ✅ **内容**: 已更新 `epDepreciationMethod` 枚举
  - `INSTANT_WRITE_OFF`
  - `STRAIGHT_LINE`
  - `DIMINISHING_BALANCE_STANDARD`
  - `DIMINISHING_BALANCE_LOW_VALUE_POOL`

### 2.2 接口定义 ⏳
- ⏳ **文件**: `equipment.type.ts`
  - 添加 `initial_depreciation_method?: string` 到 `iEquipment` 接口
  - 更新 `iCreateEquipmentRequest` 和 `iUpdateEquipmentRequest`

- ⏳ **文件**: `equipment-depreciation.type.ts`
  - 添加 `calculated_initial_data?: any` (JSONB类型)
  - 添加 `is_committed: boolean`
  - 添加 `committed_at?: Date`
  - 添加 `committed_by?: string`
  - 添加 `commit_note?: string`
  - 添加 `is_adjusted: boolean`
  - 添加 `adjustment_reason?: string`
  - 添加 `adjusted_at?: Date`
  - 添加报表生成相关接口：
    - `iGenerateDepreciationReportRequest`
    - `iGenerateDepreciationReportResponse`
    - `iCommitDepreciationReportRequest`
    - `iDepreciationReportSummary`

---

## ⏳ 阶段3：后端实现

### 3.1 更新实体类

#### 3.1.1 Equipment Entity
- ⏳ **文件**: `equipment.entity.ts`
  - 添加 `initialDepreciationMethod` 字段映射

#### 3.1.2 EquipmentDepreciationRecord Entity
- ⏳ **文件**: `equipment-depreciation-record.entity.ts`
  - 添加 `calculatedInitialData` 字段（JSONB）
  - 添加 `isCommitted`, `committedAt`, `committedBy`, `commitNote` 字段
  - 添加 `isAdjusted`, `adjustmentReason`, `adjustedAt` 字段

### 3.2 创建折旧计算工具类

- ⏳ **文件**: `depreciationCalculation.util.ts`
- ⏳ **位置**: `xituan_backend/submodules/xituan_codebase/utils/`
- ⏳ **功能**:
  - `calculateDepreciation()` - 核心计算函数
    - 实现四种折旧方法的计算逻辑：
      - `instant_write_off`: 第一年全额扣除
      - `straight_line`: 固定年折旧额 = 原始价格 / 使用年限
      - `diminishing_balance_standard`: 折旧率 = 200% / 有效使用年限
      - `diminishing_balance_low_value_pool`: 第一年37.5%，后续18.75%，余额≤$300时全额扣除
  - `determineOpeningValue()` - 确定期初价值
    - 如果有上一财年记录，使用上一年的 `closing_value`
    - 否则使用设备的 `purchase_price`
  - `determineDepreciationMethod()` - 确定折旧方法
    - 检查设备是否有已确认的记录
    - 如果有，使用 `initial_depreciation_method`
    - 如果没有，使用设备当前的 `depreciation_method`
  - `buildCalculationData()` - 构建计算数据JSON
    - 保存计算日期、版本、设备快照、计算参数、计算结果

### 3.3 创建报表服务

- ⏳ **文件**: `equipment-depreciation-report.service.ts`
- ⏳ **位置**: `xituan_backend/src/domains/equipment-depreciation/services/`
- ⏳ **功能**:
  - `generateReport()` - 生成折旧报表
    - 接收财年和可选设备ID列表
    - 遍历所有设备（或指定设备）
    - 对每个设备：
      - 检查是否已有该财年的记录
      - 如果已有且 `is_committed = true`，跳过
      - 如果已有且 `is_committed = false`，重新计算并更新
      - 如果没有，创建新记录
      - 调用折旧计算工具计算折旧
      - 保存 `calculated_initial_data`
      - 如果是首次生成记录，更新设备的 `initial_depreciation_method`
    - 返回生成结果汇总
  - `commitReport()` - 确认报表
    - 接收财年和确认信息（确认人、备注）
    - 查询该财年所有未确认的记录
    - 批量更新 `is_committed = true`，记录确认信息
    - 返回确认结果
  - `getReport()` - 获取报表
    - 接收财年参数
    - 查询该财年所有折旧记录
    - 按设备分组，计算汇总
    - 返回报表数据
  - `getReportSummary()` - 获取报表汇总
    - 计算总折旧额、设备数量、已确认数量等

### 3.4 更新Controller

- ⏳ **文件**: `equipment-depreciation.controller.ts`
- ⏳ **新增接口**:
  - `generateReport()` - POST `/api/equipment-depreciation/generate-report`
    - 验证请求参数
    - 调用报表服务生成报表
    - 返回生成结果
  - `commitReport()` - POST `/api/equipment-depreciation/commit-report`
    - 验证请求参数
    - 调用报表服务确认报表
    - 返回确认结果
  - `getReport()` - GET `/api/equipment-depreciation/report/:financialYear`
    - 获取指定财年的报表
    - 返回报表数据
  - `getReportSummary()` - GET `/api/equipment-depreciation/report/:financialYear/summary`
    - 获取报表汇总信息

### 3.5 更新Routes

- ⏳ **文件**: `equipment-depreciation.routes.ts`
- ⏳ **新增路由**:
  ```typescript
  router.post('/equipment-depreciation/generate-report', ...);
  router.post('/equipment-depreciation/commit-report', ...);
  router.get('/equipment-depreciation/report/:financialYear', ...);
  router.get('/equipment-depreciation/report/:financialYear/summary', ...);
  ```

### 3.6 更新Repository（如需要）

- ⏳ **文件**: `equipment-depreciation-record.repository.ts`
- ⏳ **新增方法**（如需要）:
  - `findByFinancialYear()` - 按财年查询
  - `findUncommittedByFinancialYear()` - 查询未确认记录
  - `updateCommitStatus()` - 批量更新确认状态

---

## ⏳ 阶段4：前端实现

### 4.1 创建API客户端

- ⏳ **文件**: `equipment-depreciation-report.api.ts`
- ⏳ **位置**: `xituan_cms/src/api/`
- ⏳ **功能**:
  - `generateDepreciationReport()` - 调用生成报表API
  - `commitDepreciationReport()` - 调用确认报表API
  - `getDepreciationReport()` - 调用获取报表API
  - `getDepreciationReportSummary()` - 调用获取汇总API

### 4.2 创建报表页面

- ⏳ **文件**: `depreciation-report.tsx`
- ⏳ **位置**: `xituan_cms/src/pages/equipment/`
- ⏳ **功能**:
  - 财年选择器
  - 生成报表按钮
  - 确认报表按钮（带确认对话框）
  - 报表数据表格显示：
    - 设备名称
    - 折旧方法
    - 期初价值
    - 折旧金额
    - 期末价值
    - 确认状态（已确认/未确认）
    - 调整状态（已调整/未调整）
    - 操作列（查看详情、编辑（仅未确认））
  - 汇总信息显示：
    - 总设备数
    - 总折旧额
    - 已确认数量
    - 未确认数量
  - 状态提示：
    - 已确认记录显示锁定图标
    - 已调整记录显示调整标识

### 4.3 创建详情Modal组件

- ⏳ **文件**: `DepreciationRecordDetailModal.tsx`
- ⏳ **位置**: `xituan_cms/src/components/equipment/`
- ⏳ **功能**:
  - 显示折旧记录详情
  - 显示计算初始数据（JSON格式化显示）
  - 显示确认信息（如果有）
  - 显示调整信息（如果有）
  - 编辑功能（仅未确认记录）：
    - 允许手动修改 `opening_value`, `depreciation_amount`, `closing_value`
    - 记录调整原因
    - 标记 `is_adjusted = true`

### 4.4 更新设备编辑Modal

- ⏳ **文件**: `EquipmentEditModal.tsx`
- ⏳ **更新内容**:
  - 折旧记录表格中显示确认状态
  - 已确认记录不可编辑（禁用编辑按钮）
  - 显示调整标识（如果有）
  - 显示 `initial_depreciation_method` 信息（只读）

### 4.5 更新设备列表页面（可选）

- ⏳ **文件**: `equipment/index.tsx`
- ⏳ **更新内容**（可选）:
  - 添加快速链接到折旧报表页面
  - 显示设备折旧状态

---

## ⏳ 阶段5：测试

### 5.1 单元测试

- ⏳ 测试折旧计算工具类
  - 测试四种折旧方法的计算准确性
  - 测试边界情况（如余额≤$300的处理）
  - 测试期初价值确定逻辑
  - 测试折旧方法确定逻辑

### 5.2 集成测试

- ⏳ 测试报表生成流程
  - 测试生成新记录
  - 测试更新未确认记录
  - 测试跳过已确认记录
  - 测试首次记录时更新 `initial_depreciation_method`

- ⏳ 测试报表确认流程
  - 测试批量确认
  - 测试确认后记录锁定

- ⏳ 测试编辑控制
  - 测试未确认记录可编辑
  - 测试已确认记录不可编辑
  - 测试调整记录标记

### 5.3 端到端测试

- ⏳ 完整流程测试
  - 创建设备 → 生成报表 → 确认报表 → 查看报表
  - 测试手动调整功能
  - 测试多财年场景

---

## 📋 实施优先级

### 高优先级（核心功能）
1. ✅ 数据库迁移
2. ✅ 枚举更新
3. ⏳ 类型定义更新
4. ⏳ 折旧计算工具类
5. ⏳ 报表服务（生成、确认、查询）
6. ⏳ Controller和Routes
7. ⏳ 前端API客户端
8. ⏳ 报表页面

### 中优先级（增强功能）
9. ⏳ 详情Modal组件
10. ⏳ 设备编辑Modal更新
11. ⏳ 单元测试

### 低优先级（优化功能）
12. ⏳ 设备列表页面更新
13. ⏳ 集成测试
14. ⏳ 端到端测试

---

## 🔍 关键注意事项

### 折旧方法一致性
- 一旦设备有已确认的折旧记录，后续财年必须使用相同的折旧方法（ATO要求）
- 系统通过 `initial_depreciation_method` 字段记录首次使用的折旧方法
- 生成报表时自动检查并使用正确的折旧方法

### 数据完整性
- `calculated_initial_data` 必须保存完整的计算信息，用于审计和追溯
- 已确认的记录（`is_committed = true`）不可修改
- 手动调整的记录必须记录调整原因

### 财年格式
- 财年格式：`YYYY-YY`（如：`2024-25`）
- 需要实现财年验证和格式化工具函数

### 计算精度
- 所有金额计算保留2位小数
- 使用 `Math.round(value * 100) / 100` 进行四舍五入

---

## 📝 开发检查清单

### 后端检查清单
- [ ] 所有实体类字段已更新
- [ ] 折旧计算工具类已实现并测试
- [ ] 报表服务已实现所有方法
- [ ] Controller已添加所有接口
- [ ] Routes已配置所有路由
- [ ] 错误处理已完善
- [ ] 日志记录已添加

### 前端检查清单
- [ ] API客户端已实现所有方法
- [ ] 报表页面已实现所有功能
- [ ] 详情Modal已实现
- [ ] 设备编辑Modal已更新
- [ ] 状态显示正确（已确认/未确认/已调整）
- [ ] 编辑控制正确（已确认记录不可编辑）
- [ ] 错误提示已完善
- [ ] 加载状态已处理

---

## 🎯 完成标准

### 功能完整性
- ✅ 所有折旧方法计算正确
- ✅ 报表生成功能正常
- ✅ 报表确认功能正常
- ✅ 编辑控制功能正常
- ✅ 数据保存和查询正常

### 代码质量
- ✅ 代码符合项目规范
- ✅ 类型定义完整
- ✅ 错误处理完善
- ✅ 注释清晰

### 用户体验
- ✅ 界面清晰易用
- ✅ 状态提示明确
- ✅ 操作反馈及时
- ✅ 错误提示友好

